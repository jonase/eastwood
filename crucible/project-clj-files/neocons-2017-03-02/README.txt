Dec 20 2013: Latest neocons passes 'lein check', but 'lein eastwood'
throws exceptions because it fails to make network connections.  Added
to lint.sh so that it only lints :source-paths, not :test-paths.
